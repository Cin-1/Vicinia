import React from 'react';
import Cards from './Cards';

const Sections = (): JSX.Element => {
  return (
    <div>
      {/* Display Contents */}
      <Cards />
    </div>
  );
};

export default Sections;
